--- a/cmake-proxies/cmake-modules/TenacityFunctions.cmake	2025-01-01 07:28:49.000000000 -0500
+++ b/cmake-proxies/cmake-modules/TenacityFunctions.cmake	2026-02-25 18:30:47.820047164 -0500
@@ -427,11 +427,11 @@
    target_link_options( ${TARGET} PRIVATE ${LOPTS} )
    target_link_libraries( ${TARGET} PUBLIC ${LIBRARIES} )
 
-   if( NOT CMAKE_SYSTEM_NAME MATCHES "Windows" )
+   if( NOT CMAKE_SYSTEM_NAME MATCHES "Windows" AND CMAKE_STRIP)
       add_custom_command(
          TARGET "${TARGET}"
          POST_BUILD
-         COMMAND $<IF:$<CONFIG:Debug>,echo,strip> -x $<TARGET_FILE:${TARGET}>
+         COMMAND $<IF:$<CONFIG:Debug>,echo,${CMAKE_STRIP}> -x $<TARGET_FILE:${TARGET}>
       )
    endif()
 
