--- a/src/ui/wxWidgets/PasswordSafeFrame.cpp	2025-11-22 04:17:08.000000000 -0500
+++ b/src/ui/wxWidgets/PasswordSafeFrame.cpp	2026-04-21 09:23:39.104615889 -0400
@@ -78,268 +78,212 @@
 
 IMPLEMENT_CLASS( PasswordSafeFrame, wxFrame )
 
-DEFINE_EVENT_TYPE(wxEVT_GUI_DB_PREFS_CHANGE)
-
 /*!
- * PasswordSafeFrame event table definition
+ * PasswordSafeFrame events definitions
  */
+wxDEFINE_EVENT(wxEVT_GUI_DB_PREFS_CHANGE, wxCommandEvent);
 
-BEGIN_EVENT_TABLE( PasswordSafeFrame, wxFrame )
-
-  EVT_CHAR_HOOK(                        PasswordSafeFrame::OnChar                        )
-  EVT_CLOSE(                            PasswordSafeFrame::OnCloseWindow                 )
-  EVT_ICONIZE(                          PasswordSafeFrame::OnIconize                     )
-
-  ////////////////////////////////////////////////////////////////////////////////////////
-  // Menu: "File"
-  ////////////////////////////////////////////////////////////////////////////////////////
-
-  // Connect event handlers
-  EVT_MENU( wxID_NEW,                   PasswordSafeFrame::OnNewClick                    )
-  EVT_MENU( wxID_OPEN,                  PasswordSafeFrame::OnOpenClick                   )
-  EVT_MENU( wxID_CLOSE,                 PasswordSafeFrame::OnCloseClick                  )
-  EVT_MENU( ID_LOCK_SAFE,               PasswordSafeFrame::OnLockSafe                    )
-  EVT_MENU( ID_UNLOCK_SAFE,             PasswordSafeFrame::OnUnlockSafe                  )
-  EVT_MENU( ID_MENU_CLEAR_MRU,          PasswordSafeFrame::OnClearRecentHistory          )
-  EVT_MENU( wxID_SAVE,                  PasswordSafeFrame::OnSaveClick                   )
-  EVT_MENU( wxID_SAVEAS,                PasswordSafeFrame::OnSaveAsClick                 )
-
-  EVT_MENU( ID_EXPORT2OLD1XFORMAT,      PasswordSafeFrame::OnExportVx                    )
-  EVT_MENU( ID_EXPORT2V2FORMAT,         PasswordSafeFrame::OnExportVx                    )
-  EVT_MENU( ID_EXPORT2V4FORMAT,         PasswordSafeFrame::OnExportVx                    )
-  EVT_MENU( ID_EXPORT2PLAINTEXT,        PasswordSafeFrame::OnExportPlainText             )
-  EVT_MENU( ID_EXPORT2XML,              PasswordSafeFrame::OnExportXml                   )
-
-  EVT_MENU( ID_IMPORT_PLAINTEXT,        PasswordSafeFrame::OnImportText                  )
-  EVT_MENU( ID_IMPORT_XML,              PasswordSafeFrame::OnImportXML                   )
-  EVT_MENU( ID_IMPORT_KEEPASS,          PasswordSafeFrame::OnImportKeePass               )
-
-  EVT_MENU( ID_MERGE,                   PasswordSafeFrame::OnMergeAnotherSafe            )
-  EVT_MENU( ID_COMPARE,                 PasswordSafeFrame::OnCompare                     )
-  EVT_MENU( ID_SYNCHRONIZE,             PasswordSafeFrame::OnSynchronize                 )
-  EVT_MENU( ID_CHANGEMODE,              PasswordSafeFrame::OnChangeMode                  )
-  EVT_MENU( wxID_PROPERTIES,            PasswordSafeFrame::OnPropertiesClick             )
-  EVT_MENU( wxID_EXIT,                  PasswordSafeFrame::OnExitClick                   )
-
-  // Update of menu items
-  EVT_UPDATE_UI( wxID_NEW,              PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( wxID_OPEN,             PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( wxID_CLOSE,            PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_LOCK_SAFE,          PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_UNLOCK_SAFE,        PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_MENU_CLEAR_MRU,     PasswordSafeFrame::OnUpdateClearRecentDBHistory  )
-  EVT_UPDATE_UI( wxID_SAVE,             PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( wxID_SAVEAS,           PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_EXPORTMENU,         PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_IMPORTMENU,         PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_IMPORT_XML,         PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_MERGE,              PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_COMPARE,            PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SYNCHRONIZE,        PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_CHANGEMODE,         PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( wxID_PROPERTIES,       PasswordSafeFrame::OnUpdateUI                    )
-  
-  ////////////////////////////////////////////////////////////////////////////////////////
-  // Menu: "Edit"
-  ////////////////////////////////////////////////////////////////////////////////////////
-  
-  // Connect event handlers
-  EVT_MENU( wxID_ADD,                   PasswordSafeFrame::OnAddClick                    )
-  EVT_MENU( ID_EDIT,                    PasswordSafeFrame::OnEditClick                   )
-  EVT_MENU( wxID_DELETE,                PasswordSafeFrame::OnDeleteClick                 )
-  EVT_MENU( wxID_FIND,                  PasswordSafeFrame::OnFindClick                   )
-  EVT_MENU( ID_DUPLICATEENTRY,          PasswordSafeFrame::OnDuplicateEntry              )
-  EVT_MENU( ID_PROTECT,                 PasswordSafeFrame::OnProtectUnprotectClick       )
-
-  EVT_MENU( wxID_UNDO,                  PasswordSafeFrame::OnUndo                        )
-  EVT_MENU( wxID_REDO,                  PasswordSafeFrame::OnRedo                        )
-  EVT_MENU( ID_CLEARCLIPBOARD,          PasswordSafeFrame::OnClearClipboardClick         )
-  EVT_MENU( ID_COPYPASSWORD,            PasswordSafeFrame::OnCopyPasswordClick           )
-  EVT_MENU( ID_COPYUSERNAME,            PasswordSafeFrame::OnCopyUsernameClick           )
-  EVT_MENU( ID_COPYAUTHCODE,            PasswordSafeFrame::OnCopyAuthCodeClick           )
-  EVT_MENU( ID_SHOWAUTHCODE,            PasswordSafeFrame::OnShowAuthCodeClick           )
-  EVT_MENU( ID_COPYNOTESFLD,            PasswordSafeFrame::OnCopyNotesFieldClick         )
-  EVT_MENU( ID_COPYURL,                 PasswordSafeFrame::OnCopyUrlClick                )
-  EVT_MENU( ID_BROWSEURL,               PasswordSafeFrame::OnBrowseUrl                   )
-  EVT_MENU( ID_AUTOTYPE,                PasswordSafeFrame::OnAutoType                    )
-  EVT_MENU( ID_VIEWATTACHMENT,          PasswordSafeFrame::OnViewAttachment              )
-  EVT_MENU( ID_GOTOBASEENTRY,           PasswordSafeFrame::OnGotoBase                    )
-
-  // Update menu items
-  EVT_UPDATE_UI( wxID_ADD,              PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_EDIT,               PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( wxID_DELETE,           PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( wxID_FIND,             PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_RENAME,             PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_DUPLICATEENTRY,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_PROTECT,            PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_ADDGROUP,           PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( wxID_UNDO,             PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( wxID_REDO,             PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_CLEARCLIPBOARD,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_COPYPASSWORD,       PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_COPYUSERNAME,       PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_COPYAUTHCODE,       PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SHOWAUTHCODE,       PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_COPYNOTESFLD,       PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_COPYURL,            PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_BROWSEURL,          PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_AUTOTYPE,           PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_VIEWATTACHMENT,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_GOTOBASEENTRY,      PasswordSafeFrame::OnUpdateUI                    )
-
-  ////////////////////////////////////////////////////////////////////////////////////////
-  // Menu: "View"
-  ////////////////////////////////////////////////////////////////////////////////////////
-
-  // Connect event handlers
-  EVT_MENU( ID_LIST_VIEW,               PasswordSafeFrame::OnListViewClick               )
-  EVT_MENU( ID_TREE_VIEW,               PasswordSafeFrame::OnTreeViewClick               )
-  EVT_MENU( ID_SORT_TREE_BY_GROUP,      PasswordSafeFrame::OnSortByGroupClick            )
-  EVT_MENU( ID_SORT_TREE_BY_NAME,       PasswordSafeFrame::OnSortByNameClick             )
-  EVT_MENU( ID_SORT_TREE_BY_DATE,       PasswordSafeFrame::OnSortByDateClick             )
-  EVT_MENU( ID_SHOWHIDE_TOOLBAR,        PasswordSafeFrame::OnShowHideToolBar             )
-  EVT_MENU( ID_TOOLBAR_NEW,             PasswordSafeFrame::OnChangeToolbarType           )
-  EVT_MENU( ID_TOOLBAR_CLASSIC,         PasswordSafeFrame::OnChangeToolbarType           )
-  EVT_MENU( ID_SHOWHIDE_DRAGBAR,        PasswordSafeFrame::OnShowHideDragBar             )
-  EVT_MENU( ID_EXPANDALL,               PasswordSafeFrame::OnExpandAll                   )
-  EVT_MENU( ID_COLLAPSEALL,             PasswordSafeFrame::OnCollapseAll                 )
-  EVT_MENU( ID_SHOWHIDE_UNSAVED,        PasswordSafeFrame::OnShowUnsavedEntriesClick     )
-  EVT_MENU( ID_SHOW_ALL_EXPIRY,         PasswordSafeFrame::OnShowAllExpiryClick          )
-  EVT_MENU( ID_SHOW_LAST_FIND_RESULTS,  PasswordSafeFrame::OnShowLastFindClick           )
-  EVT_MENU( ID_EDITFILTER,              PasswordSafeFrame::OnEditFilter                  )
-  EVT_MENU( ID_APPLYFILTER,             PasswordSafeFrame::OnApplyFilter                 )
-  EVT_MENU( ID_MANAGEFILTERS,           PasswordSafeFrame::OnManageFilters               )
-  EVT_MENU( ID_SHOW_EMPTY_GROUP_IN_FILTER, PasswordSafeFrame::OnShowGroupInFilterClick   )
-  EVT_MENU( ID_CHANGETREEFONT,          PasswordSafeFrame::OnChangeTreeFont              )
-  EVT_MENU( ID_CHANGEADDEDITFONT,       PasswordSafeFrame::OnChangeAddEditFont           )
-  EVT_MENU( ID_CHANGEPSWDFONT,          PasswordSafeFrame::OnChangePasswordFont          )
-  EVT_MENU( ID_CHANGENOTESFONT,         PasswordSafeFrame::OnChangeNotesFont             )
-  EVT_MENU( ID_CHANGEVKBFONT,           PasswordSafeFrame::OnChangeVirtualKeyboardFont   )
-  EVT_MENU( ID_REPORT_COMPARE,          PasswordSafeFrame::OnShowReportCompare           )
-  EVT_MENU( ID_REPORT_SYNCHRONIZE,      PasswordSafeFrame::OnShowReportSynchronize       )
-  EVT_MENU( ID_REPORT_MERGE,            PasswordSafeFrame::OnShowReportMerge             )
-  EVT_MENU( ID_REPORT_IMPORTTEXT,       PasswordSafeFrame::OnShowReportImportText        )
-  EVT_MENU( ID_REPORT_IMPORTXML,        PasswordSafeFrame::OnShowReportImportXML         )
-  EVT_MENU( ID_REPORT_IMPORTKEEPASS_TXT, PasswordSafeFrame::OnShowReportImportKeePassV1_TXT )
-  EVT_MENU( ID_REPORT_IMPORTKEEPASS_CSV, PasswordSafeFrame::OnShowReportImportKeePassV1_CSV )
-  EVT_MENU( ID_REPORT_EXPORTTEXT,       PasswordSafeFrame::OnShowReportExportText        )
-  EVT_MENU( ID_REPORT_EXPORTXML,        PasswordSafeFrame::OnShowReportExportXML         )
-  EVT_MENU( ID_REPORT_EXPORT_DB,        PasswordSafeFrame::OnShowReportExportDB          )
-  EVT_MENU( ID_REPORT_FIND,             PasswordSafeFrame::OnShowReportFind              )
-  EVT_MENU( ID_REPORT_VALIDATE,         PasswordSafeFrame::OnShowReportValidate          )
-  // Update menu items
-  EVT_UPDATE_UI( ID_LIST_VIEW,          PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_TREE_VIEW,          PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SORT_TREE_MENU,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SORT_TREE_BY_GROUP, PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SORT_TREE_BY_NAME,  PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SORT_TREE_BY_DATE,  PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SHOWHIDE_TOOLBAR,   PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SHOWHIDE_DRAGBAR,   PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SUBVIEWSMENU,       PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SHOWHIDE_UNSAVED,   PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SHOW_ALL_EXPIRY,    PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SHOW_LAST_FIND_RESULTS, PasswordSafeFrame::OnUpdateUI                )
-  EVT_UPDATE_UI( ID_COLLAPSEALL,        PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_EXPANDALL,          PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_FILTERMENU,         PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_EDITFILTER,         PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_APPLYFILTER,        PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_MANAGEFILTERS,      PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SHOW_EMPTY_GROUP_IN_FILTER, PasswordSafeFrame::OnUpdateUI            )
-  EVT_UPDATE_UI( ID_CHANGETREEFONT,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_CHANGEADDEDITFONT,  PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_CHANGEPSWDFONT,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_CHANGENOTESFONT,    PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_CHANGEVKBFONT,      PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_CHANGEFONTMENU,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_CUSTOMIZETOOLBAR,   PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORTSMENU,        PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_COMPARE,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_SYNCHRONIZE, PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_MERGE,       PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_IMPORTTEXT,  PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_IMPORTXML,   PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_IMPORTKEEPASS_TXT, PasswordSafeFrame::OnUpdateUI              )
-  EVT_UPDATE_UI( ID_REPORT_IMPORTKEEPASS_CSV, PasswordSafeFrame::OnUpdateUI              )
-  EVT_UPDATE_UI( ID_REPORT_EXPORTTEXT,  PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_EXPORTXML,   PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_EXPORT_DB,   PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_FIND,        PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_REPORT_VALIDATE,    PasswordSafeFrame::OnUpdateUI                    )
-
-  ////////////////////////////////////////////////////////////////////////////////////////
-  // Menu: "Manage"
-  ////////////////////////////////////////////////////////////////////////////////////////
-
-  // Connect event handlers
-  EVT_MENU( ID_CHANGECOMBO,             PasswordSafeFrame::OnChangePasswordClick         )
-  EVT_MENU( ID_BACKUP,                  PasswordSafeFrame::OnBackupSafe                  )
-  EVT_MENU( ID_RESTORE,                 PasswordSafeFrame::OnRestoreSafe                 )
-  EVT_MENU( wxID_PREFERENCES,           PasswordSafeFrame::OnPreferencesClick            )
-  EVT_MENU( ID_PWDPOLSM,                PasswordSafeFrame::OnPwdPolsMClick               )
-  EVT_MENU( ID_GENERATEPASSWORD,        PasswordSafeFrame::OnGeneratePassword            )
+void PasswordSafeFrame::BindEvents()
+{
+  Bind(wxEVT_CHAR_HOOK,    &PasswordSafeFrame::OnChar,        this);
+  Bind(wxEVT_CLOSE_WINDOW, &PasswordSafeFrame::OnCloseWindow, this);
+  Bind(wxEVT_ICONIZE,      &PasswordSafeFrame::OnIconize,     this);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnNewClick, this, wxID_NEW);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnOpenClick, this, wxID_OPEN);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCloseClick, this, wxID_CLOSE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnLockSafe, this, ID_LOCK_SAFE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnUnlockSafe, this, ID_UNLOCK_SAFE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnClearRecentHistory, this, ID_MENU_CLEAR_MRU);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnSaveClick, this, wxID_SAVE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnSaveAsClick, this, wxID_SAVEAS);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnExportVx, this, ID_EXPORT2OLD1XFORMAT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnExportVx, this, ID_EXPORT2V2FORMAT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnExportVx, this, ID_EXPORT2V4FORMAT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnExportPlainText, this, ID_EXPORT2PLAINTEXT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnExportXml, this, ID_EXPORT2XML);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnImportText, this, ID_IMPORT_PLAINTEXT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnImportXML, this, ID_IMPORT_XML);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnImportKeePass, this, ID_IMPORT_KEEPASS);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnMergeAnotherSafe, this, ID_MERGE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCompare, this, ID_COMPARE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnSynchronize, this, ID_SYNCHRONIZE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnChangeMode, this, ID_CHANGEMODE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnPropertiesClick, this, wxID_PROPERTIES);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnExitClick, this, wxID_EXIT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_NEW);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_OPEN);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_CLOSE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_LOCK_SAFE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_UNLOCK_SAFE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateClearRecentDBHistory, this, ID_MENU_CLEAR_MRU);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_SAVE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_SAVEAS);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_EXPORTMENU);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_IMPORTMENU);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_IMPORT_XML);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_MERGE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_COMPARE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SYNCHRONIZE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CHANGEMODE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_PROPERTIES);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnAddClick, this, wxID_ADD);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnEditClick, this, ID_EDIT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnDeleteClick, this, wxID_DELETE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnFindClick, this, wxID_FIND);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnDuplicateEntry, this, ID_DUPLICATEENTRY);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnProtectUnprotectClick, this, ID_PROTECT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnUndo, this, wxID_UNDO);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnRedo, this, wxID_REDO);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnClearClipboardClick, this, ID_CLEARCLIPBOARD);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCopyPasswordClick, this, ID_COPYPASSWORD);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCopyUsernameClick, this, ID_COPYUSERNAME);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCopyAuthCodeClick, this, ID_COPYAUTHCODE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowAuthCodeClick, this, ID_SHOWAUTHCODE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCopyNotesFieldClick, this, ID_COPYNOTESFLD);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCopyUrlClick, this, ID_COPYURL);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnBrowseUrl, this, ID_BROWSEURL);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnAutoType, this, ID_AUTOTYPE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnViewAttachment, this, ID_VIEWATTACHMENT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnGotoBase, this, ID_GOTOBASEENTRY);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_ADD);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_EDIT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_DELETE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_FIND);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_RENAME);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_DUPLICATEENTRY);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_PROTECT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_ADDGROUP);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_UNDO);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_REDO);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CLEARCLIPBOARD);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_COPYPASSWORD);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_COPYUSERNAME);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_COPYAUTHCODE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SHOWAUTHCODE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_COPYNOTESFLD);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_COPYURL);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_BROWSEURL);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_AUTOTYPE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_VIEWATTACHMENT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_GOTOBASEENTRY);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnListViewClick, this, ID_LIST_VIEW);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnTreeViewClick, this, ID_TREE_VIEW);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnSortByGroupClick, this, ID_SORT_TREE_BY_GROUP);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnSortByNameClick, this, ID_SORT_TREE_BY_NAME);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnSortByDateClick, this, ID_SORT_TREE_BY_DATE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowHideToolBar, this, ID_SHOWHIDE_TOOLBAR);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnChangeToolbarType, this, ID_TOOLBAR_NEW);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnChangeToolbarType, this, ID_TOOLBAR_CLASSIC);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowHideDragBar, this, ID_SHOWHIDE_DRAGBAR);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnExpandAll, this, ID_EXPANDALL);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCollapseAll, this, ID_COLLAPSEALL);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowUnsavedEntriesClick, this, ID_SHOWHIDE_UNSAVED);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowAllExpiryClick, this, ID_SHOW_ALL_EXPIRY);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowLastFindClick, this, ID_SHOW_LAST_FIND_RESULTS);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnEditFilter, this, ID_EDITFILTER);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnApplyFilter, this, ID_APPLYFILTER);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnManageFilters, this, ID_MANAGEFILTERS);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowGroupInFilterClick, this, ID_SHOW_EMPTY_GROUP_IN_FILTER);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnChangeTreeFont, this, ID_CHANGETREEFONT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnChangeAddEditFont, this, ID_CHANGEADDEDITFONT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnChangePasswordFont, this, ID_CHANGEPSWDFONT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnChangeNotesFont, this, ID_CHANGENOTESFONT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnChangeVirtualKeyboardFont, this, ID_CHANGEVKBFONT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportCompare, this, ID_REPORT_COMPARE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportSynchronize, this, ID_REPORT_SYNCHRONIZE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportMerge, this, ID_REPORT_MERGE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportImportText, this, ID_REPORT_IMPORTTEXT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportImportXML, this, ID_REPORT_IMPORTXML);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportImportKeePassV1_TXT, this, ID_REPORT_IMPORTKEEPASS_TXT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportImportKeePassV1_CSV, this, ID_REPORT_IMPORTKEEPASS_CSV);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportExportText, this, ID_REPORT_EXPORTTEXT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportExportXML, this, ID_REPORT_EXPORTXML);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportExportDB, this, ID_REPORT_EXPORT_DB);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportFind, this, ID_REPORT_FIND);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnShowReportValidate, this, ID_REPORT_VALIDATE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_LIST_VIEW);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_TREE_VIEW);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SORT_TREE_MENU);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SORT_TREE_BY_GROUP);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SORT_TREE_BY_NAME);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SORT_TREE_BY_DATE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SHOWHIDE_TOOLBAR);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SHOWHIDE_DRAGBAR);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SUBVIEWSMENU);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SHOWHIDE_UNSAVED);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SHOW_ALL_EXPIRY);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SHOW_LAST_FIND_RESULTS);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_COLLAPSEALL);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_EXPANDALL);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_FILTERMENU);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_EDITFILTER);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_APPLYFILTER);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_MANAGEFILTERS);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SHOW_EMPTY_GROUP_IN_FILTER);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CHANGETREEFONT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CHANGEADDEDITFONT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CHANGEPSWDFONT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CHANGENOTESFONT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CHANGEVKBFONT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CHANGEFONTMENU);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CUSTOMIZETOOLBAR);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORTSMENU);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_COMPARE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_SYNCHRONIZE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_MERGE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_IMPORTTEXT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_IMPORTXML);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_IMPORTKEEPASS_TXT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_IMPORTKEEPASS_CSV);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_EXPORTTEXT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_EXPORTXML);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_EXPORT_DB);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_FIND);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_REPORT_VALIDATE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnChangePasswordClick, this, ID_CHANGECOMBO);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnBackupSafe, this, ID_BACKUP);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnRestoreSafe, this, ID_RESTORE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnPreferencesClick, this, wxID_PREFERENCES);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnPwdPolsMClick, this, ID_PWDPOLSM);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnGeneratePassword, this, ID_GENERATEPASSWORD);
 #ifndef NO_YUBI
-  EVT_MENU( ID_YUBIKEY_MNG,             PasswordSafeFrame::OnYubikeyMngClick             )
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnYubikeyMngClick, this, ID_YUBIKEY_MNG);
 #endif
-  EVT_MENU_RANGE( ID_LANGUAGE_BEGIN, ID_LANGUAGE_END, PasswordSafeFrame::OnLanguageClick )
-
-  // Update menu elements
-  EVT_UPDATE_UI( ID_CHANGECOMBO,        PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_BACKUP,             PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_RESTORE,            PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( wxID_PREFERENCES,      PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_PWDPOLSM,           PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_GENERATEPASSWORD,   PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SETDATABASEID,      PasswordSafeFrame::OnUpdateUI                    )
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnLanguageClick, this, ID_LANGUAGE_BEGIN, ID_LANGUAGE_END);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CHANGECOMBO);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_BACKUP);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_RESTORE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, wxID_PREFERENCES);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_PWDPOLSM);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_GENERATEPASSWORD);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SETDATABASEID);
 #ifndef NO_YUBI
-  EVT_UPDATE_UI( ID_YUBIKEY_MNG,        PasswordSafeFrame::OnUpdateUI                    )
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_YUBIKEY_MNG);
 #endif
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnVisitWebsite, this, ID_VISITWEBSITE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnAboutClick, this, wxID_ABOUT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnPasswordSubset, this, ID_PASSWORDSUBSET);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnPasswordQRCode, this, ID_PASSWORDQRCODE);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCopyEmailClick, this, ID_COPYEMAIL);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCopyRunCmd, this, ID_COPYRUNCOMMAND);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnBrowseUrlAndAutotype, this, ID_BROWSEURLPLUS);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnSendEmail, this, ID_SENDEMAIL);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnRunCommand, this, ID_RUNCOMMAND);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnCreateShortcut, this, ID_CREATESHORTCUT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnEditBase, this, ID_EDITBASEENTRY);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_PASSWORDSUBSET);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_PASSWORDQRCODE);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_COPYEMAIL);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_BROWSEURLPLUS);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_SENDEMAIL);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_RUNCOMMAND);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_COPYRUNCOMMAND);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_CREATESHORTCUT);
+  Bind(wxEVT_UPDATE_UI, &PasswordSafeFrame::OnUpdateUI, this, ID_EDITBASEENTRY);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnFindNext, this, ID_EDITMENU_FIND_NEXT);
+  Bind(wxEVT_MENU, &PasswordSafeFrame::OnFindPrevious, this, ID_EDITMENU_FIND_PREVIOUS);
+}
 
-  ////////////////////////////////////////////////////////////////////////////////////////
-  // Menu: "Help"
-  ////////////////////////////////////////////////////////////////////////////////////////
-
-  // Connect event handlers
-  EVT_MENU( ID_VISITWEBSITE,            PasswordSafeFrame::OnVisitWebsite                )
-  EVT_MENU( wxID_ABOUT,                 PasswordSafeFrame::OnAboutClick                  )
-
-  ////////////////////////////////////////////////////////////////////////////////////////
-  // Context Menu
-  ////////////////////////////////////////////////////////////////////////////////////////
-
-  // Connect event handlers
-  EVT_MENU( ID_PASSWORDSUBSET,          PasswordSafeFrame::OnPasswordSubset              )
-  EVT_MENU( ID_PASSWORDQRCODE,          PasswordSafeFrame::OnPasswordQRCode              )
-  EVT_MENU( ID_COPYEMAIL,               PasswordSafeFrame::OnCopyEmailClick              )
-  EVT_MENU( ID_COPYRUNCOMMAND,          PasswordSafeFrame::OnCopyRunCmd                  )
-  EVT_MENU( ID_BROWSEURLPLUS,           PasswordSafeFrame::OnBrowseUrlAndAutotype        )
-  EVT_MENU( ID_SENDEMAIL,               PasswordSafeFrame::OnSendEmail                   )
-  EVT_MENU( ID_RUNCOMMAND,              PasswordSafeFrame::OnRunCommand                  )
-  EVT_MENU( ID_CREATESHORTCUT,          PasswordSafeFrame::OnCreateShortcut              )
-  EVT_MENU( ID_EDITBASEENTRY,           PasswordSafeFrame::OnEditBase                    )
-
-  // Update menu elements
-  EVT_UPDATE_UI( ID_PASSWORDSUBSET,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_PASSWORDQRCODE,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_COPYEMAIL,          PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_BROWSEURLPLUS,      PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_SENDEMAIL,          PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_RUNCOMMAND,         PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_COPYRUNCOMMAND,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_CREATESHORTCUT,     PasswordSafeFrame::OnUpdateUI                    )
-  EVT_UPDATE_UI( ID_EDITBASEENTRY,      PasswordSafeFrame::OnUpdateUI                    )
-
-  ////////////////////////////////////////////////////////////////////////////////////////
-  // Search Control
-  ////////////////////////////////////////////////////////////////////////////////////////
-
-  // Connect event handlers
-  EVT_MENU( ID_EDITMENU_FIND_NEXT,      PasswordSafeFrame::OnFindNext                    )
-  EVT_MENU( ID_EDITMENU_FIND_PREVIOUS,  PasswordSafeFrame::OnFindPrevious                )
-
-END_EVENT_TABLE()
 
 int PasswordSafeFrame::s_TotpCountdownInterval = 1000; // ms
 int PasswordSafeFrame::s_TotpCalculationInterval = 30; // s
@@ -401,6 +345,7 @@
   m_AuiManager.SetManagedWindow(this);
   CreateMenubar();
   CreateControls();
+  BindEvents();
   SetIcon(GetIconResource(L"graphics/cpane.xpm"));
   Centre();
 ////@end PasswordSafeFrame creation
@@ -853,7 +798,7 @@
   wxLanguage system_language = wxGetApp().GetSystemLanguage();
 
   for (auto &item : m_languages) {
-    wxString lang_name = _(get<1>(item.second));
+    wxString lang_name = wxGetTranslation(get<1>(item.second));
     if (get<0>(item.second) == system_language) {
       lang_name = L"[ " + lang_name + L" ]";
     }
@@ -956,17 +901,17 @@
       if(toolbarShowText)
         toolbar->AddTool(
           PwsToolbarButton.id,
-          wxGetTranslation(PwsToolbarButton.toollabel),
+          PwsToolbarButton.GetToolLabel(),
           PwsToolbarButton.GetBitmapForEnabledButton(),
           PwsToolbarButton.GetBitmapForDisabledButton(),
           wxITEM_NORMAL,
-          wxGetTranslation(PwsToolbarButton.tooltip),
+          PwsToolbarButton.GetToolTip(),
           wxEmptyString,
           nullptr);
       else
         toolbar->AddTool(
           PwsToolbarButton.id,
-          wxGetTranslation(PwsToolbarButton.toollabel),
+          PwsToolbarButton.GetToolLabel(),
           PwsToolbarButton.GetBitmapForEnabledButton(),
           PwsToolbarButton.GetBitmapForDisabledButton(),
           wxITEM_NORMAL,
@@ -1453,8 +1398,16 @@
 int PasswordSafeFrame::DoOpen(const wxString& title)
 {
   stringT dir = PWSdirs::GetSafeDir();
+
+  // On GTK/wxWidgets in Termux/Android, preselecting a default filename can
+  // trigger a GTK file chooser path that AddressSanitizer reports as a UAF.
+  // Avoid preselecting a filename; just set the directory.
+  wxString defaultFile = wxEmptyString;
+#ifndef __ANDROID__
+  defaultFile = wxT("pwsafe.psafe4");
+#endif
   //Open-type dialog box
-  wxFileDialog fd(this, title, dir.c_str(), wxT("pwsafe.psafe4"),
+  wxFileDialog fd(this, title, dir.c_str(), defaultFile,
                   _("Password Safe Databases (*.psafe4; *.psafe3; *.dat)|*.psafe4;*.psafe3;*.dat|Password Safe Backups (*.bak)|*.bak|Password Safe Intermediate Backups (*.ibak)|*.ibak|All files (*.*; *)|*.*;*"),
                   (wxFD_OPEN | wxFD_FILE_MUST_EXIST | wxFD_CHANGE_DIR));
 
@@ -3021,7 +2974,7 @@
       menu->SetItemLabel(m_core.IsReadOnly() ? _("Change to R/W") : _("Change to R-O"));
   }
   else { // no open file
-    m_statusBar->SetStatusText(_(PWSprefs::GetDCAdescription(-1)), StatusBar::Field::DOUBLECLICK);
+    m_statusBar->SetStatusText(wxGetTranslation(PWSprefs::GetDCAdescription(-1)), StatusBar::Field::DOUBLECLICK);
     m_statusBar->SetStatusText(wxEmptyString, StatusBar::Field::CLIPBOARDACTION);
     m_statusBar->SetStatusText(wxEmptyString, StatusBar::Field::MODIFIED);
     m_statusBar->SetStatusText(wxEmptyString, StatusBar::Field::READONLY);
@@ -3103,7 +3056,7 @@
     if (dca == -1)
       dca = PWSprefs::GetInstance()->GetPref(PWSprefs::DoubleClickAction);
   }
-  m_statusBar->SetStatusText(_(PWSprefs::GetDCAdescription(dca)), StatusBar::Field::DOUBLECLICK);
+  m_statusBar->SetStatusText(wxGetTranslation(PWSprefs::GetDCAdescription(dca)), StatusBar::Field::DOUBLECLICK);
 }
 
 void PasswordSafeFrame::ChangeFontPreference(const PWSprefs::StringPrefs fontPreference)
